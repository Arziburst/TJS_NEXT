/** @type {import('postcss-load-config').Config} */
const config = {
    plugins: {
        tailwindcss:                          {},
        'postcss-viewport-height-correction': {},
        autoprefixer:                         {},
    },
};

export default config;
